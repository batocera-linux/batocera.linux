From: ROCKNIX <local>
Subject: drm/msm: harden put_iova_spaces and GEM/dmabuf teardown (v7.0+)

drm_gpuvm_bo_find() for vm!=NULL avoids walking obj->gpuva.list twice
(close/KMS/unpin paths). dma_resv_wait before the final msm_gem_vma_put
in msm_gem_close; dma_resv_wait in msm_gem_dmabuf_release. Snapshot VMA
pointers for teardown. Check msm_gem_lock_vm_and_obj() return value.

diff --git a/drivers/gpu/drm/msm/msm_gem.c b/drivers/gpu/drm/msm/msm_gem.c
--- a/drivers/gpu/drm/msm/msm_gem.c
+++ b/drivers/gpu/drm/msm/msm_gem.c
@@ -53,25 +53,29 @@
 {
 	struct msm_context *ctx = file->driver_priv;
 	struct drm_exec exec;
+	int ret;
 
 	update_ctx_mem(file, -obj->size);
-	msm_gem_vma_put(obj);
 
 	/*
 	 * If VM isn't created yet, nothing to cleanup.  And in fact calling
 	 * put_iova_spaces() with vm=NULL would be bad, in that it will tear-
 	 * down the mappings of shared buffers in other contexts.
 	 */
-	if (!ctx->vm)
+	if (!ctx->vm) {
+		msm_gem_vma_put(obj);
 		return;
+	}
 
 	/*
 	 * VM_BIND does not depend on implicit teardown of VMAs on handle
 	 * close, but instead on implicit teardown of the VM when the device
 	 * is closed (see msm_gem_vm_close())
 	 */
-	if (msm_context_is_vmbind(ctx))
+	if (msm_context_is_vmbind(ctx)) {
+		msm_gem_vma_put(obj);
 		return;
+	}
 
 	/*
 	 * TODO we might need to kick this to a queue to avoid blocking
@@ -80,8 +84,14 @@
 	dma_resv_wait_timeout(obj->resv, DMA_RESV_USAGE_BOOKKEEP, false,
 			      MAX_SCHEDULE_TIMEOUT);
 
-	msm_gem_lock_vm_and_obj(&exec, obj, ctx->vm);
-	put_iova_spaces(obj, ctx->vm, true, "close");
+	msm_gem_vma_put(obj);
+
+	ret = msm_gem_lock_vm_and_obj(&exec, obj, ctx->vm);
+	if (!ret)
+		put_iova_spaces(obj, ctx->vm, true, "close");
+	else
+		WARN(1, "msm_gem_close: lock failed (%d), skipping IOVA teardown\n",
+		     ret);
 	drm_exec_fini(&exec);     /* drop locks */
 }
 
@@ -106,9 +116,14 @@
 
 #ifdef CONFIG_DRM_MSM_KMS
 	struct drm_exec exec;
+	int ret;
 
-	msm_gem_lock_vm_and_obj(&exec, obj, priv->kms->vm);
-	put_iova_spaces(obj, priv->kms->vm, true, "vma_put");
+	ret = msm_gem_lock_vm_and_obj(&exec, obj, priv->kms->vm);
+	if (!ret)
+		put_iova_spaces(obj, priv->kms->vm, true, "vma_put");
+	else
+		WARN(1, "msm_gem_vma_put: lock failed (%d), skipping IOVA teardown\n",
+		     ret);
 	drm_exec_fini(&exec);     /* drop locks */
 #endif
 }
@@ -411,26 +426,101 @@
 put_iova_spaces(struct drm_gem_object *obj, struct drm_gpuvm *vm,
 		bool close, const char *reason)
 {
-	struct drm_gpuvm_bo *vm_bo, *tmp;
+	struct drm_gpuvm_bo *vm_bo, **vm_bos = NULL;
+	struct drm_gpuva *vma, **vas;
+	unsigned int i, n, match, j, nv;
 
 	msm_gem_assert_locked(obj);
 
-	drm_gem_for_each_gpuvm_bo_safe (vm_bo, tmp, obj) {
-		struct drm_gpuva *vma, *vmatmp;
+	/*
+	 * Single-VM teardown (handle close, unpin, etc.): look up the vm_bo
+	 * directly instead of walking obj->gpuva.list. That list can still be
+	 * walked for purge/evict/free (vm == NULL) where every VM must be hit.
+	 */
+	if (vm) {
+		vm_bo = drm_gpuvm_bo_find(vm, obj);
+		if (!vm_bo)
+			return;
+
+		nv = 0;
+		drm_gpuvm_bo_for_each_va(vma, vm_bo)
+			nv++;
+		if (!nv) {
+			drm_gpuvm_bo_put(vm_bo);
+			return;
+		}
 
-		if (vm && vm_bo->vm != vm)
-			continue;
+		vas = kmalloc_array(nv, sizeof(*vas), GFP_KERNEL);
+		if (!vas) {
+			WARN(1, "msm: put_iova_spaces vma OOM (nv=%u)\n", nv);
+			drm_gpuvm_bo_put(vm_bo);
+			return;
+		}
 
-		drm_gpuvm_bo_get(vm_bo);
+		j = 0;
+		drm_gpuvm_bo_for_each_va(vma, vm_bo)
+			vas[j++] = vma;
 
-		drm_gpuvm_bo_for_each_va_safe (vma, vmatmp, vm_bo) {
-			msm_gem_vma_unmap(vma, reason);
+		for (j = 0; j < nv; j++) {
+			msm_gem_vma_unmap(vas[j], reason);
 			if (close)
-				msm_gem_vma_close(vma);
+				msm_gem_vma_close(vas[j]);
+		}
+		kfree(vas);
+		drm_gpuvm_bo_put(vm_bo);
+		return;
+	}
+
+	match = 0;
+	drm_gem_for_each_gpuvm_bo(vm_bo, obj)
+		match++;
+	if (!match)
+		return;
+
+	vm_bos = kmalloc_array(match, sizeof(*vm_bos), GFP_KERNEL);
+	if (!vm_bos) {
+		WARN(1, "msm: put_iova_spaces OOM (match=%u)\n", match);
+		return;
+	}
+
+	n = 0;
+	drm_gem_for_each_gpuvm_bo(vm_bo, obj)
+		vm_bos[n++] = drm_gpuvm_bo_get(vm_bo);
+	GEM_WARN_ON(n != match);
+
+	for (i = 0; i < n; i++) {
+		vm_bo = vm_bos[i];
+
+		nv = 0;
+		drm_gpuvm_bo_for_each_va(vma, vm_bo)
+			nv++;
+		if (!nv) {
+			drm_gpuvm_bo_put(vm_bo);
+			continue;
+		}
+
+		vas = kmalloc_array(nv, sizeof(*vas), GFP_KERNEL);
+		if (!vas) {
+			WARN(1, "msm: put_iova_spaces vma snapshot OOM (nv=%u)\n", nv);
+			drm_gpuvm_bo_put(vm_bo);
+			continue;
 		}
 
+		j = 0;
+		drm_gpuvm_bo_for_each_va(vma, vm_bo)
+			vas[j++] = vma;
+		GEM_WARN_ON(j != nv);
+
+		for (j = 0; j < nv; j++) {
+			msm_gem_vma_unmap(vas[j], reason);
+			if (close)
+				msm_gem_vma_close(vas[j]);
+		}
+		kfree(vas);
 		drm_gpuvm_bo_put(vm_bo);
 	}
+
+	kfree(vm_bos);
 }
 
 static struct drm_gpuva *get_vma_locked(struct drm_gem_object *obj,
@@ -556,8 +646,10 @@
 	struct drm_exec exec;
 	int ret;
 
-	msm_gem_lock_vm_and_obj(&exec, obj, vm);
-	ret = get_and_pin_iova_range_locked(obj, vm, iova, range_start, range_end);
+	ret = msm_gem_lock_vm_and_obj(&exec, obj, vm);
+	if (!ret)
+		ret = get_and_pin_iova_range_locked(obj, vm, iova, range_start,
+						    range_end);
 	drm_exec_fini(&exec);     /* drop locks */
 
 	return ret;
@@ -579,14 +671,16 @@
 {
 	struct drm_gpuva *vma;
 	struct drm_exec exec;
-	int ret = 0;
+	int ret;
 
-	msm_gem_lock_vm_and_obj(&exec, obj, vm);
-	vma = get_vma_locked(obj, vm, 0, U64_MAX);
-	if (IS_ERR(vma)) {
-		ret = PTR_ERR(vma);
-	} else {
-		*iova = vma->va.addr;
+	ret = msm_gem_lock_vm_and_obj(&exec, obj, vm);
+	if (!ret) {
+		vma = get_vma_locked(obj, vm, 0, U64_MAX);
+		if (IS_ERR(vma)) {
+			ret = PTR_ERR(vma);
+		} else {
+			*iova = vma->va.addr;
+		}
 	}
 	drm_exec_fini(&exec);     /* drop locks */
 
@@ -620,17 +714,20 @@
 	struct drm_exec exec;
 	int ret = 0;
 
-	msm_gem_lock_vm_and_obj(&exec, obj, vm);
-	if (!iova) {
-		ret = clear_iova(obj, vm);
-	} else {
-		struct drm_gpuva *vma;
-		vma = get_vma_locked(obj, vm, iova, iova + obj->size);
-		if (IS_ERR(vma)) {
-			ret = PTR_ERR(vma);
-		} else if (GEM_WARN_ON(vma->va.addr != iova)) {
-			clear_iova(obj, vm);
-			ret = -EBUSY;
+	ret = msm_gem_lock_vm_and_obj(&exec, obj, vm);
+	if (!ret) {
+		if (!iova) {
+			ret = clear_iova(obj, vm);
+		} else {
+			struct drm_gpuva *vma;
+
+			vma = get_vma_locked(obj, vm, iova, iova + obj->size);
+			if (IS_ERR(vma)) {
+				ret = PTR_ERR(vma);
+			} else if (GEM_WARN_ON(vma->va.addr != iova)) {
+				clear_iova(obj, vm);
+				ret = -EBUSY;
+			}
 		}
 	}
 	drm_exec_fini(&exec);     /* drop locks */
@@ -658,14 +755,18 @@
 {
 	struct drm_gpuva *vma;
 	struct drm_exec exec;
+	int ret;
 
-	msm_gem_lock_vm_and_obj(&exec, obj, vm);
-	vma = lookup_vma(obj, vm);
-	if (vma) {
-		msm_gem_unpin_locked(obj);
+	ret = msm_gem_lock_vm_and_obj(&exec, obj, vm);
+	if (!ret) {
+		vma = lookup_vma(obj, vm);
+		if (vma)
+			msm_gem_unpin_locked(obj);
+		if (!is_kms_vm(vm))
+			put_iova_spaces(obj, vm, true, "close");
+	} else {
+		WARN(1, "msm_gem_unpin_iova: lock failed (%d)\n", ret);
 	}
-	if (!is_kms_vm(vm))
-		put_iova_spaces(obj, vm, true, "close");
 	drm_exec_fini(&exec);     /* drop locks */
 }
 

diff --git a/drivers/gpu/drm/msm/msm_gem_prime.c b/drivers/gpu/drm/msm/msm_gem_prime.c
--- a/drivers/gpu/drm/msm/msm_gem_prime.c
+++ b/drivers/gpu/drm/msm/msm_gem_prime.c
@@ -5,6 +5,7 @@
  */
 
 #include <linux/dma-buf.h>
+#include <linux/dma-resv.h>
 
 #include <drm/drm_drv.h>
 #include <drm/drm_prime.h>
@@ -47,6 +48,9 @@
 {
 	struct drm_gem_object *obj = dma_buf->priv;
 
+	dma_resv_wait_timeout(obj->resv, DMA_RESV_USAGE_BOOKKEEP, false,
+			      MAX_SCHEDULE_TIMEOUT);
+
 	msm_gem_vma_put(obj);
 	drm_gem_dmabuf_release(dma_buf);
 }
