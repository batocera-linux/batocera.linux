Subject: [PATCH] leds: aw200xx: add optional vdd-supply regulator

Add an optional vdd-supply matching the pattern already used
by leds-lp8860.c and leds-lp8864.c.
---
 drivers/leds/leds-aw200xx.c | 5 +++++
 1 file changed, 5 insertions(+)

diff --git a/drivers/leds/leds-aw200xx.c b/drivers/leds/leds-aw200xx.c
--- a/drivers/leds/leds-aw200xx.c
+++ b/drivers/leds/leds-aw200xx.c
@@ -18,6 +18,7 @@
 #include <linux/module.h>
 #include <linux/mutex.h>
 #include <linux/regmap.h>
+#include <linux/regulator/consumer.h>
 #include <linux/time.h>
 #include <linux/units.h>

@@ -567,6 +568,10 @@ static int aw200xx_probe(struct i2c_client *client)
 	if (IS_ERR(chip->regmap))
 		return PTR_ERR(chip->regmap);

+	ret = devm_regulator_get_enable_optional(&client->dev, "vdd");
+	if (ret < 0 && ret != -ENODEV)
+		return dev_err_probe(&client->dev, ret, "Failed to enable vdd supply\n");
+
 	chip->hwen = devm_gpiod_get_optional(&client->dev, "enable",
 					     GPIOD_OUT_HIGH);
 	if (IS_ERR(chip->hwen))

