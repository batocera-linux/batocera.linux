From: sunshineinabox <aqemail@gmail.com>
Subject: [PATCH] drm/msm: add SM4450 display support (DPU + MDSS + UBWC)

---
diff -Nur a/drivers/gpu/drm/msm/disp/dpu1/catalog/dpu_8_6_sm4450.h b/drivers/gpu/drm/msm/disp/dpu1/catalog/dpu_8_6_sm4450.h
--- a/drivers/gpu/drm/msm/disp/dpu1/catalog/dpu_8_6_sm4450.h	1969-12-31 16:00:00
+++ b/drivers/gpu/drm/msm/disp/dpu1/catalog/dpu_8_6_sm4450.h	2026-09-05 01:24:16
@@ -0,0 +1,162 @@
+/* SPDX-License-Identifier: GPL-2.0-only */
+/*
+ * Copyright (c) 2026, The Linux Foundation. All rights reserved.
+ *
+ * SM4450 DPU catalog, derived from the vendor devicetree of the
+ * Retroid Pocket Classic and from dpu_8_1_sm8450.h.
+ *
+ */
+
+#ifndef _DPU_8_6_SM4450_H
+#define _DPU_8_6_SM4450_H
+
+static const struct dpu_caps sm4450_dpu_caps = {
+	.max_mixer_width = DEFAULT_DPU_OUTPUT_LINE_WIDTH,
+	.max_mixer_blendstages = 0x5,
+	.has_dim_layer = true,
+	.has_idle_pc = true,
+	.max_linewidth = 4096,
+	.pixel_ram_size = DEFAULT_PIXEL_RAM_SIZE,
+};
+
+static const struct dpu_mdp_cfg sm4450_mdp = {
+	.name = "top_0",
+	.base = 0x0, .len = 0x494,
+	.clk_ctrls = {
+		[DPU_CLK_CTRL_VIG0] = { .reg_off = 0x2ac, .bit_off = 0 },
+		[DPU_CLK_CTRL_DMA0] = { .reg_off = 0x2ac, .bit_off = 8 },
+	},
+};
+
+static const struct dpu_ctl_cfg sm4450_ctl[] = {
+	{
+		.name = "ctl_0", .id = CTL_0,
+		.base = 0x15000, .len = 0x204,
+		.intr_start = DPU_IRQ_IDX(MDP_SSPP_TOP0_INTR2, 9),
+	},
+};
+
+static const struct dpu_sspp_cfg sm4450_sspp[] = {
+	{
+		.name = "sspp_0", .id = SSPP_VIG0,
+		.base = 0x4000, .len = 0x32c,
+		.features = VIG_SC7280_MASK_SDMA,
+		.sblk = &dpu_vig_sblk_qseed3_3_1_rot_v201,
+		.xin_id = 0,
+		.type = SSPP_TYPE_VIG,
+		.clk_ctrl = DPU_CLK_CTRL_VIG0,
+	}, {
+		.name = "sspp_8", .id = SSPP_DMA0,
+		.base = 0x24000, .len = 0x32c,
+		.features = DMA_SDM845_MASK_SDMA,
+		.sblk = &dpu_dma_sblk,
+		.xin_id = 1,
+		.type = SSPP_TYPE_DMA,
+		.clk_ctrl = DPU_CLK_CTRL_DMA0,
+	},
+};
+
+static const struct dpu_lm_cfg sm4450_lm[] = {
+	{
+		.name = "lm_0", .id = LM_0,
+		.base = 0x44000, .len = 0x400,
+		.sblk = &sm4450_lm_sblk,
+		.pingpong = PINGPONG_0,
+		.dspp = DSPP_0,
+	},
+};
+
+static const struct dpu_dspp_cfg sm4450_dspp[] = {
+	{
+		.name = "dspp_0", .id = DSPP_0,
+		.base = 0x54000, .len = 0x1800,
+		.sblk = &sdm845_dspp_sblk,
+	},
+};
+
+static const struct dpu_pingpong_cfg sm4450_pp[] = {
+	{
+		.name = "pingpong_0", .id = PINGPONG_0,
+		.base = 0x69000, .len = 0,
+		.sblk = &sc7280_pp_sblk,
+		.intr_done = DPU_IRQ_IDX(MDP_SSPP_TOP0_INTR, 8),
+	},
+};
+
+static const struct dpu_dsc_cfg sm4450_dsc[] = {
+	{
+		.name = "dce_0_0", .id = DSC_0,
+		.base = 0x80000, .len = 0x4,
+		.sblk = &dsc_sblk_0,
+	},
+};
+
+static const struct dpu_intf_cfg sm4450_intf[] = {
+	{
+		.name = "intf_1", .id = INTF_1,
+		.base = 0x35000, .len = 0x2c4,
+		.type = INTF_DSI,
+		.controller_id = MSM_DSI_CONTROLLER_0,
+		.prog_fetch_lines_worst_case = 24,
+		.intr_underrun = DPU_IRQ_IDX(MDP_SSPP_TOP0_INTR, 26),
+		.intr_vsync = DPU_IRQ_IDX(MDP_SSPP_TOP0_INTR, 27),
+		.intr_tear_rd_ptr = DPU_IRQ_IDX(MDP_INTF1_TEAR_INTR, 2),
+	},
+};
+
+static const struct dpu_perf_cfg sm4450_perf_data = {
+	.max_bw_low = 4200000,
+	.max_bw_high = 7000000,
+	.min_core_ib = 2500000,
+	.min_llcc_ib = 0,
+	.min_dram_ib = 1600000,
+	.min_prefill_lines = 40,
+	.danger_lut_tbl = {0x3fffff, 0x3fffff, 0x0},
+	.safe_lut_tbl = {0xf800, 0xf800, 0x0},
+	.qos_lut_tbl = {
+		{.nentry = ARRAY_SIZE(sm4450_qos_linear_macrotile),
+		.entries = sm4450_qos_linear_macrotile
+		},
+		{.nentry = ARRAY_SIZE(sm4450_qos_linear_macrotile),
+		.entries = sm4450_qos_linear_macrotile
+		},
+		{.nentry = ARRAY_SIZE(sc7180_qos_nrt),
+		.entries = sc7180_qos_nrt
+		},
+	},
+	.cdp_cfg = {
+		{.rd_enable = 1, .wr_enable = 1},
+		{.rd_enable = 1, .wr_enable = 0}
+	},
+	.clk_inefficiency_factor = 105,
+	.bw_inefficiency_factor = 120,
+};
+
+static const struct dpu_mdss_version sm4450_mdss_ver = {
+	.core_major_ver = 8,
+	.core_minor_ver = 6,
+};
+
+const struct dpu_mdss_cfg dpu_sm4450_cfg = {
+	.mdss_ver = &sm4450_mdss_ver,
+	.caps = &sm4450_dpu_caps,
+	.mdp = &sm4450_mdp,
+	.ctl_count = ARRAY_SIZE(sm4450_ctl),
+	.ctl = sm4450_ctl,
+	.sspp_count = ARRAY_SIZE(sm4450_sspp),
+	.sspp = sm4450_sspp,
+	.dspp_count = ARRAY_SIZE(sm4450_dspp),
+	.dspp = sm4450_dspp,
+	.mixer_count = ARRAY_SIZE(sm4450_lm),
+	.mixer = sm4450_lm,
+	.pingpong_count = ARRAY_SIZE(sm4450_pp),
+	.pingpong = sm4450_pp,
+	.dsc_count = ARRAY_SIZE(sm4450_dsc),
+	.dsc = sm4450_dsc,
+	.intf_count = ARRAY_SIZE(sm4450_intf),
+	.intf = sm4450_intf,
+	.vbif = &sdm845_vbif,
+	.perf = &sm4450_perf_data,
+};
+
+#endif
diff -Nur a/drivers/gpu/drm/msm/disp/dpu1/dpu_hw_catalog.c b/drivers/gpu/drm/msm/disp/dpu1/dpu_hw_catalog.c
--- a/drivers/gpu/drm/msm/disp/dpu1/dpu_hw_catalog.c	2026-09-05 01:24:16
+++ b/drivers/gpu/drm/msm/disp/dpu1/dpu_hw_catalog.c	2026-09-05 01:24:16
@@ -190,6 +190,12 @@
 	/* TODO add formats after validation */
 };
 
+static const u32 rotation_v201_formats[] = {
+	DRM_FORMAT_NV12,
+	DRM_FORMAT_ABGR8888,
+	DRM_FORMAT_ABGR2101010,
+};
+
 static const u32 wb2_formats_rgb_yuv[] = {
 	DRM_FORMAT_RGB565,
 	DRM_FORMAT_BGR565,
@@ -309,6 +317,12 @@
 	.rot_format_list = rotation_v2_formats,
 };
 
+static const struct dpu_rotation_cfg dpu_rot_sm4450_cfg_v201 = {
+	.rot_maxheight = 1200,
+	.rot_num_formats = ARRAY_SIZE(rotation_v201_formats),
+	.rot_format_list = rotation_v201_formats,
+};
+
 static const struct dpu_sspp_sub_blks dpu_vig_sblk_qseed2 =
 				_VIG_SBLK_QSEED2();
 
@@ -334,6 +348,10 @@
 			_VIG_SBLK_ROT(SSPP_SCALER_VER(3, 0),
 				      &dpu_rot_sc7280_cfg_v2);
 
+static const struct dpu_sspp_sub_blks dpu_vig_sblk_qseed3_3_1_rot_v201 =
+			_VIG_SBLK_ROT(SSPP_SCALER_VER(3, 1),
+				      &dpu_rot_sm4450_cfg_v201);
+
 static const struct dpu_sspp_sub_blks dpu_vig_sblk_qseed3_3_1 =
 				_VIG_SBLK(SSPP_SCALER_VER(3, 1));
 
@@ -380,6 +398,13 @@
 	},
 };
 
+static const struct dpu_lm_sub_blks sm4450_lm_sblk = {
+	.maxblendstages = 5, /* excluding base layer */
+	.blendstage_base = { /* offsets relative to mixer base */
+		0x20, 0x38, 0x50, 0x68, 0x80
+	},
+};
+
 static const struct dpu_lm_sub_blks sm8750_lm_sblk = {
 	.maxblendstages = 11, /* excluding base layer */
 	.blendstage_base = { /* offsets relative to mixer base */
@@ -740,6 +765,10 @@
 	{.fl = 0, .lut = 0x0},
 };
 
+static const struct dpu_qos_lut_entry sm4450_qos_linear_macrotile[] = {
+	{.fl = 0, .lut = 0x0122334445566777},
+};
+
 /*************************************************************
  * Hardware catalog
  *************************************************************/
@@ -775,6 +805,7 @@
 #include "catalog/dpu_8_0_sc8280xp.h"
 #include "catalog/dpu_8_1_sm8450.h"
 #include "catalog/dpu_8_4_sa8775p.h"
+#include "catalog/dpu_8_6_sm4450.h"
 
 #include "catalog/dpu_9_0_sm8550.h"
 #include "catalog/dpu_9_1_sar2130p.h"
diff -Nur a/drivers/gpu/drm/msm/disp/dpu1/dpu_hw_catalog.h b/drivers/gpu/drm/msm/disp/dpu1/dpu_hw_catalog.h
--- a/drivers/gpu/drm/msm/disp/dpu1/dpu_hw_catalog.h	2026-09-05 01:24:16
+++ b/drivers/gpu/drm/msm/disp/dpu1/dpu_hw_catalog.h	2026-09-05 01:24:16
@@ -793,6 +793,7 @@
 extern const struct dpu_mdss_cfg dpu_sc8280xp_cfg;
 extern const struct dpu_mdss_cfg dpu_sm8450_cfg;
 extern const struct dpu_mdss_cfg dpu_sa8775p_cfg;
+extern const struct dpu_mdss_cfg dpu_sm4450_cfg;
 extern const struct dpu_mdss_cfg dpu_sm8550_cfg;
 extern const struct dpu_mdss_cfg dpu_sm8650_cfg;
 extern const struct dpu_mdss_cfg dpu_sm8750_cfg;
diff -Nur a/drivers/gpu/drm/msm/disp/dpu1/dpu_kms.c b/drivers/gpu/drm/msm/disp/dpu1/dpu_kms.c
--- a/drivers/gpu/drm/msm/disp/dpu1/dpu_kms.c	2026-09-05 01:24:16
+++ b/drivers/gpu/drm/msm/disp/dpu1/dpu_kms.c	2026-09-05 01:24:16
@@ -1500,6 +1500,7 @@
 	{ .compatible = "qcom,sc7280-dpu", .data = &dpu_sc7280_cfg, },
 	{ .compatible = "qcom,sc8180x-dpu", .data = &dpu_sc8180x_cfg, },
 	{ .compatible = "qcom,sc8280xp-dpu", .data = &dpu_sc8280xp_cfg, },
+	{ .compatible = "qcom,sm4450-dpu", .data = &dpu_sm4450_cfg, },
 	{ .compatible = "qcom,sm6115-dpu", .data = &dpu_sm6115_cfg, },
 	{ .compatible = "qcom,sm6125-dpu", .data = &dpu_sm6125_cfg, },
 	{ .compatible = "qcom,sm6150-dpu", .data = &dpu_sm6150_cfg, },
diff -Nur a/drivers/gpu/drm/msm/msm_mdss.c b/drivers/gpu/drm/msm/msm_mdss.c
--- a/drivers/gpu/drm/msm/msm_mdss.c	2026-09-05 01:24:16
+++ b/drivers/gpu/drm/msm/msm_mdss.c	2026-09-05 01:24:16
@@ -541,6 +541,7 @@
 	{ .compatible = "qcom,sc7280-mdss", .data = &data_74k },
 	{ .compatible = "qcom,sc8180x-mdss", .data = &data_76k8 },
 	{ .compatible = "qcom,sc8280xp-mdss", .data = &data_76k8 },
+	{ .compatible = "qcom,sm4450-mdss", .data = &data_74k },
 	{ .compatible = "qcom,sm6115-mdss", .data = &data_76k8 },
 	{ .compatible = "qcom,sm6125-mdss", .data = &data_76k8 },
 	{ .compatible = "qcom,sm6150-mdss", .data = &data_76k8 },
diff -Nur a/drivers/soc/qcom/ubwc_config.c b/drivers/soc/qcom/ubwc_config.c
--- a/drivers/soc/qcom/ubwc_config.c	2026-09-05 01:24:16
+++ b/drivers/soc/qcom/ubwc_config.c	2026-09-05 01:24:16
@@ -133,6 +133,16 @@
 	.highest_bank_bit = 15,
 };
 
+static const struct qcom_ubwc_cfg_data sm4450_data = {
+	.ubwc_enc_version = UBWC_2_0,
+	.ubwc_dec_version = UBWC_2_0,
+	.ubwc_swizzle = UBWC_SWIZZLE_ENABLE_LVL2 |
+			UBWC_SWIZZLE_ENABLE_LVL3,
+	.ubwc_bank_spread = true,
+	/* LPDDR4X */
+	.highest_bank_bit = 14,
+};
+
 static const struct qcom_ubwc_cfg_data sm6115_data = {
 	.ubwc_enc_version = UBWC_1_0,
 	.ubwc_dec_version = UBWC_2_0,
@@ -279,6 +289,7 @@
 	{ .compatible = "qcom,sdm670", .data = &sdm670_data, },
 	{ .compatible = "qcom,sdm845", .data = &sdm845_data, },
 	{ .compatible = "qcom,sm4250", .data = &sm6115_data, },
+	{ .compatible = "qcom,sm4450", .data = &sm4450_data, },
 	{ .compatible = "qcom,sm6115", .data = &sm6115_data, },
 	{ .compatible = "qcom,sm6125", .data = &sm6125_data, },
 	{ .compatible = "qcom,sm6150", .data = &sm6150_data, },
