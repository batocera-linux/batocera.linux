# xpad is built into the kernel (CONFIG_JOYSTICK_XPAD=y) so this modprobe.d
# file has no effect on module loading. auto_poweroff must be disabled at
# runtime via /sys/bus/usb/drivers/xpad/module/parameters/auto_poweroff.
# options xpad auto_poweroff=N
