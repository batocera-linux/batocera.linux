These Device Tree Overlay files (dtbo) are required for panel variants on different boards.
To activate the overlay just renamed the chosen .dtbo to `mipi-panel.dtbo`
This will allow the chosen display to function at boot.
