[project]
name = "batocera-launch-openmohaa"
version = '44.0'
dependencies = [
    'batocera-common',
    'batocera-launch',
]

[project.entry-points."batocera_launch.emulators"]
openmohaa = 'batocera_launch_openmohaa:OpenMOHAA'

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["/batocera_launch_openmohaa"]
