[project]
name = "batocera-launch-mame-common"
version = '44.0'
dependencies = [
    'batocera-common',
    'batocera-launch',
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["/batocera_launch_mame_common"]
