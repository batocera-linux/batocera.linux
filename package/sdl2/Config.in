config BR2_PACKAGE_SDL2
	bool "SDL2"
	depends on BR2_PACKAGE_HAS_UDEV
	help
	  Simple DirectMedia Layer - Version 2
	  SDL is a library that allows programs portable low level
	  access to a video framebuffer, audio output, mouse, and keyboard.

	  http://www.libsdl.org/
