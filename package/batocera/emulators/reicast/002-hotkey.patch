diff --git a/libswirl/input/gamepad_device.cpp b/libswirl/input/gamepad_device.cpp
index 4df5cb4..de16b90 100644
--- a/libswirl/input/gamepad_device.cpp
+++ b/libswirl/input/gamepad_device.cpp
@@ -30,6 +30,8 @@ extern u16 kcode[4];
 extern u8 rt[4], lt[4];
 extern s8 joyx[4], joyy[4];
 
+static int batocera_hotkey = 0;
+
 std::vector<std::shared_ptr<GamepadDevice>> GamepadDevice::_gamepads;
 cMutex GamepadDevice::_gamepads_mutex;
 
@@ -79,6 +81,10 @@ bool GamepadDevice::gamepad_btn_input(u32 code, bool pressed)
 			case DC_DPAD2_RIGHT:
 				kcode[_maple_port] |= (u16)DC_DPAD2_LEFT;
 				break;
+				case DC_BTN_START:
+				if ((pressed) && (batocera_hotkey == 1))
+						virtualDreamcast->Exit();	
+ 			break;
 			default:
 				break;
 			}
@@ -93,7 +99,9 @@ bool GamepadDevice::gamepad_btn_input(u32 code, bool pressed)
 		case EMU_BTN_ESCAPE:
             verify(virtualDreamcast != nullptr);
             if (pressed)
-                virtualDreamcast->Exit();
+                batocera_hotkey = 1;
+							else
+								batocera_hotkey = 0;
 			break;
 		case EMU_BTN_MENU:
 			if (pressed)
