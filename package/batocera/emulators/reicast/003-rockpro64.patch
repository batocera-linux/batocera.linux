diff --git a/reicast/linux/configure-platform.make b/reicast/linux/configure-platform.make
index f845230..bbda769 100644
--- a/reicast/linux/configure-platform.make
+++ b/reicast/linux/configure-platform.make
@@ -165,6 +165,16 @@ else ifneq (,$(findstring odroid,$(platform)))
         endif
     endif
 
+# ROCKPRO64
+else ifneq (,$(findstring rockpro64,$(platform)))
+    MFLAGS += -marm -mfpu=neon -mfloat-abi=hard -funroll-loops -march=armv7-a
+    ASFLAGS += -mfpu=neon -mfloat-abi=hard -march=armv7-a
+    CFLAGS += -D TARGET_LINUX_ARMELv7 -DARM_HARDFP -fsingle-precision-constant
+    USE_GLES := 1
+    LIBS += -lrt -ldl
+    ARM32_REC := 1
+    INCS += -I../linux-deps/include
+
 # GCW Zero
 else ifneq (,$(findstring gcwz,$(platform)))
     NOT_ARM := 1
