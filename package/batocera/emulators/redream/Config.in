config BR2_PACKAGE_REDREAM
	bool "redream"
        depends on BR2_x86_64
	# Wine unconditionally builds shared libraries
	depends on !BR2_STATIC_LIBS
	help
          Redream
          Is a dreamcast emulateur for windows / mac / linux.

          https://redream.io

comment "redream needs a toolchain w/ dynamic library"
	depends on BR2_STATIC_LIBS
