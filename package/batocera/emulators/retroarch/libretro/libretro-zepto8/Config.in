config BR2_PACKAGE_LIBRETRO_ZEPTO8
        bool "libretro-zepto8"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
        help
          A libretro Pico-8 engine

comment "LIBRETRO_ZEPTO8 needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP

