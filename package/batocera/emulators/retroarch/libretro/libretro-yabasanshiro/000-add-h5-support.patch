diff --git a/yabause/src/libretro/Makefile b/yabause/src/libretro/Makefile
index c6e75bd74..6efc4c77f 100644
--- a/yabause/src/libretro/Makefile
+++ b/yabause/src/libretro/Makefile
@@ -147,6 +147,25 @@ else ifneq (,$(findstring rockpro64,$(platform)))
 	DYNAREC_DEVMIYAX = 1
 	FLAGS += -march=armv8-a+crc -mcpu=cortex-a72 -mtune=cortex-a72.cortex-a53 -mfloat-abi=hard -mfpu=neon-vfpv4 -mvectorize-with-neon-quad
 
+# Allwinner H5
+else ifeq ($(platform), h5)
+	override platform += unix
+	TARGET := $(TARGET_NAME)_libretro.so
+	fpic := -fPIC
+	SHARED := -shared -Wl,--no-undefined -Wl,--version-script=link.T
+	LDFLAGS += -lpthread
+	FLAGS += -DAARCH64
+	ARCH_IS_LINUX = 1
+	HAVE_SSE = 0
+	FORCE_GLES = 1
+	USE_AARCH64_DRC = 1
+	DYNAREC_DEVMIYAX = 1
+#	ifneq (, $(findstring clang,$(CC)))
+#		FLAGS += -march=armv8-a+crc
+#	else
+		FLAGS += -march=armv8-a+crc -mtune=cortex-a53
+#	endif
+
 # ODROIDs
 else ifeq ($(platform), odroid-n2)
 	override platform += unix
