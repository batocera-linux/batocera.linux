diff --git a/Makefile b/Makefile
index 193773a..ae6da9d 100644
--- a/Makefile
+++ b/Makefile
@@ -186,6 +186,17 @@ ifneq (,$(findstring unix,$(platform)))
          CPUFLAGS += -mcpu=cortex-a9 -mfpu=neon
       endif
    endif
+
+   # RockPro64
+   ifneq (,$(findstring rockpro64,$(platform)))
+      GLES = 1
+      GL_LIB := -lGLESv2
+      CPUFLAGS += -DNO_ASM -DARM -D__arm__ -DARM_ASM -D__NEON_OPT -DNOSSE
+      CPUFLAGS += -marm -mfloat-abi=hard -mfpu=neon
+      HAVE_NEON = 1
+      WITH_DYNAREC=arm
+      CPUFLAGS += -march=armv7ve -mcpu=cortex-a72 -mtune=cortex-a72.cortex-a53
+   endif
    
    # Classic Platforms ####################
    # Platform affix = classic_<ISA>_<µARCH>
