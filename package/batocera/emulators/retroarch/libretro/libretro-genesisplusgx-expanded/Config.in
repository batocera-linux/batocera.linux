config BR2_PACKAGE_LIBRETRO_GENESISPLUSGX_EXPANDED
    bool "libretro-genesisplusgx-expanded"
	depends on BR2_INSTALL_LIBSTDCPP
    help
      A libretro SEGA 8 / 16 bits emulator core with expanded rom support.

	  http://www.libretro.com

comment "LIBRETRO_GENESISPLUSGX needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
