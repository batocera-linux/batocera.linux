--- a/Makefile.libretro
+++ b/Makefile.libretro
@@ -100,6 +100,8 @@
          endif
       else ifneq (,$(findstring rpi4,$(platform)))
             PLATFORM_DEFINES += -mcpu=cortex-a72 -mtune=cortex-a72
+      else ifneq (,$(findstring rpi5,$(platform)))
+         PLATFORM_DEFINES += -DARM -mcpu=cortex-a76 -mtune=cortex-a76
       endif
    endif
