config BR2_PACKAGE_LIBRETRO_2048
	bool "libretro-2048"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  A libretro port of 2048, a game.

	  http://www.libretro.com

comment "LIBRETRO_2048 needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
