config BR2_PACKAGE_LIBRETRO_SAMEDUCK
	bool "libretro-sameduck"
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  A libretro Mega Duck / Cougar Boy core.

	  http://www.libretro.com

comment "LIBRETRO_SAMEDUCK needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
