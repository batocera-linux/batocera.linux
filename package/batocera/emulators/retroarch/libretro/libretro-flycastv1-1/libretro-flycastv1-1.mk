################################################################################
#
# LIBRETRO-FLYCASTV1_1
#
################################################################################
# version.: Commits on Oct 04, 2021
LIBRETRO_FLYCASTV1_1_VERSION = 4a913e063c95d1fae98afc64645831de0bcad57e
LIBRETRO_FLYCASTV1_1_SITE = $(call github,libretro,flycast,$(LIBRETRO_FLYCASTV1_1_VERSION))
LIBRETRO_FLYCASTV1_1_LICENSE = GPLv2
LIBRETRO_FLYCASTV1_1_DEPENDENCIES = retroarch

LIBRETRO_FLYCASTV1_1_PLATFORM = $(LIBRETRO_PLATFORM)
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS = HAVE_OPENMP=1

# LIBRETRO_PLATFORM is not good for this core, because
# for rpi, it contains "unix rpi" and this core do an "if unix elif rpi ..."

# special cases for special plateform then...
# an other proper way may be to redo the Makefile to do "if rpi elif unix ..." (from specific to general)
# the Makefile imposes that the platform has gles (or force FORCE_GLES is set) to not link with lGL

ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_BCM2711),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = rpi4_64-neon
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += FORCE_GLES=1 ARCH=arm64 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_BCM2837),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = rpi3_64-neon
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += FORCE_GLES=1 ARCH=arm64 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_XU4),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = odroid
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += BOARD=ODROID-XU4 FORCE_GLES=1 ARCH=arm LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_TRITIUM_H5),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = h5
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm64 FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_ORANGEPI_ZERO2),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = h616
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm64 FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_S905),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = AMLGX
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm64 FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_S912),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = AMLGXM
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm64 FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_S905GEN2),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = mali-drm-gles2
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm64 FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_S905GEN3),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = odroidc4
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm64 FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_RK3399),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = rpi-rpi4_64
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm64 FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_S922X),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = odroid-n2
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm64 FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_RK3326_ANY),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = arm64
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm64 FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_ORANGEPI_PC),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = sun8i
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_S812),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = odroid
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm FORCE_GLES=1 LDFLAGS=-lrt

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_RK3288),y)
LIBRETRO_FLYCASTV1_1_PLATFORM = tinkerboard
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=arm

else ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_X86),y)
LIBRETRO_FLYCASTV1_1_EXTRA_ARGS += ARCH=x86
endif

# Define the info files here because you can't do that in target_cmd.
ifeq ($(BR2_PACKAGE_BATOCERA_TARGET_X86)$(BR2_PACKAGE_BATOCERA_TARGET_RK3288),y)
	LIBRETRO_FLYCASTV1_1_INFO_FILE = flycastv1-1_libretro.info
else
	LIBRETRO_FLYCASTV1_1_INFO_FILE = flycastv1-1_gles2_libretro.info
endif

define LIBRETRO_FLYCASTV1_1_BUILD_CMDS
    $(TARGET_CONFIGURE_OPTS) $(MAKE) CXX="$(TARGET_CXX)" CC="$(TARGET_CC)" -C $(@D)/ -f Makefile \
		platform="$(LIBRETRO_FLYCASTV1_1_PLATFORM)" $(LIBRETRO_FLYCASTV1_1_EXTRA_ARGS) \
        GIT_VERSION="-$(shell echo $(LIBRETRO_FLYCASTV1_1_VERSION) | cut -c 1-7)"
endef

define LIBRETRO_FLYCASTV1_1_INSTALL_TARGET_CMDS
	$(INSTALL) -D $(@D)/flycast_libretro.so \
		$(TARGET_DIR)/usr/lib/libretro/flycastv1-1_libretro.so
	cp "$(BR2_EXTERNAL_BATOCERA_PATH)/package/batocera/emulators/retroarch/libretro/libretro-flycastv1-1/$(LIBRETRO_FLYCASTV1_1_INFO_FILE)" "$(TARGET_DIR)/usr/share/libretro/info/flycastv1-1_libretro.info"
endef

$(eval $(generic-package))
