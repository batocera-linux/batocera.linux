diff --git a/tasks/task_autodetect.c b/tasks/task_autodetect.c
index a137766..4e81839 100644
--- a/tasks/task_autodetect.c
+++ b/tasks/task_autodetect.c
@@ -693,6 +693,8 @@ static void input_autoconfigure_connect_handler(retro_task_t *task)
       task_set_flags(task, RETRO_TASK_FLG_FINISHED, true);
       return;
    }
+   
+   goto task_finished;
 
    /* Annoyingly, we have to scan all the autoconfig
     * files (and in-built configs) in a single shot
@@ -808,6 +810,7 @@ static void input_autoconfigure_connect_handler(retro_task_t *task)
       RARCH_LOG("[Autoconf] %s.\n", task_title);
    }
 
+task_finished:
    task_set_flags(task, RETRO_TASK_FLG_FINISHED, true);
 }
 
