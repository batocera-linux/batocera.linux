diff --git a/src/Applewin.cpp b/src/Applewin.cpp
index 7082f3e..3568a0f 100644
--- a/src/Applewin.cpp
+++ b/src/Applewin.cpp
@@ -885,7 +885,7 @@ void LoadAllConfigurations(const char *userSpecifiedFilename)
   if (home) {
     // Suppport old locations under HOME.
     configDir.push_back(std::string(home) + "/linapple/linapple.conf");
-    configDir.push_back(std::string(home) + "/.linapple/linapple.conf");
+    configDir.push_back(std::string(home) + "/configs/linapple/linapple.conf");
   }
 
   std::string lastSuccessfulUserConfig;
