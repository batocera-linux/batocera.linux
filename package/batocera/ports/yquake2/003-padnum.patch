diff --git a/src/client/input/sdl2.c b/src/client/input/sdl2.c
index 527e3f5c..1f36bd7d 100644
--- a/src/client/input/sdl2.c
+++ b/src/client/input/sdl2.c
@@ -2519,7 +2519,7 @@ static void
 IN_Controller_Init(qboolean notify_user)
 {
 	cvar_t *cvar;
-	int nummappings;
+	int nummappings, numjoysticks, joy_num, i;
 	char controllerdb[MAX_OSPATH] = {0};
 	SDL_Joystick *joystick = NULL;
 	SDL_bool is_controller = SDL_FALSE;
@@ -2541,7 +2541,8 @@ IN_Controller_Init(qboolean notify_user)
 	}
 
 	cvar = Cvar_Get("in_initjoy", "1", CVAR_NOSET);
-	if (!cvar->value)
+	joy_num = (int)cvar->value;
+	if (joy_num < 1)
 	{
 		return;
 	}
@@ -2571,9 +2572,10 @@ IN_Controller_Init(qboolean notify_user)
 		}
 	}
 
-	Com_Printf ("%i joysticks were found.\n", SDL_NumJoysticks());
+	numjoysticks = SDL_NumJoysticks();
+	Com_Printf ("%i joysticks were found.\n", numjoysticks);
 
-	if (!SDL_NumJoysticks())
+	if (numjoysticks == 0)
 	{
 		joystick_haptic = SDL_HapticOpenFromMouse();
 
@@ -2602,7 +2604,10 @@ IN_Controller_Init(qboolean notify_user)
 			Com_Printf ("%d mappings loaded from gamecontrollerdb.txt\n", nummappings);
 	}
 
-	for (int i = 0; i < SDL_NumJoysticks(); i++)
+	if (joy_num > numjoysticks) joy_num = 1;
+	joy_num--;
+	i = joy_num;
+	do
 	{
 		const char* joystick_name;
 		size_t name_len;
@@ -2611,7 +2616,7 @@ IN_Controller_Init(qboolean notify_user)
 		if (!joystick)
 		{
 			Com_Printf ("Couldn't open joystick %d: %s.\n", i+1, SDL_GetError());
-			continue;	// try next joystick
+			goto next_joy;	// try next joystick
 		}
 
 		joystick_name = SDL_JoystickName(joystick);
@@ -2651,7 +2656,7 @@ IN_Controller_Init(qboolean notify_user)
 				Com_Printf ("skipping.\n");
 			}
 #endif
-			continue;
+			goto next_joy;
 		}
 
 		Com_Printf ("Buttons = %d, Axes = %d, Hats = %d\n", SDL_JoystickNumButtons(joystick),
@@ -2680,7 +2685,7 @@ IN_Controller_Init(qboolean notify_user)
 			if (!controller)
 			{
 				Com_Printf("SDL Controller error: %s.\n", SDL_GetError());
-				continue;	// try next joystick
+				goto next_joy;	// try next joystick
 			}
 
 			show_gamepad = true;
@@ -2769,7 +2774,12 @@ IN_Controller_Init(qboolean notify_user)
 			break;
 #endif
 		}
+
+next_joy:
+		i++;
+		if (i == numjoysticks) i = 0;
 	}
+	while (i != joy_num);
 
 	IN_GamepadLabels_Changed();
 	IN_GamepadConfirm_Changed();
diff --git a/src/client/input/sdl3.c b/src/client/input/sdl3.c
index 5817e88e..e208aff6 100644
--- a/src/client/input/sdl3.c
+++ b/src/client/input/sdl3.c
@@ -2500,7 +2500,7 @@ static void
 IN_Controller_Init(qboolean notify_user)
 {
 	cvar_t *cvar;
-	int nummappings;
+	int nummappings, numjoysticks, joy_num, i;
 	char controllerdb[MAX_OSPATH] = {0};
 	SDL_Joystick *joystick = NULL;
 	bool is_controller = false;
@@ -2522,7 +2522,8 @@ IN_Controller_Init(qboolean notify_user)
 	}
 
 	cvar = Cvar_Get("in_initjoy", "1", CVAR_NOSET);
-	if (!cvar->value)
+	joy_num = (int)cvar->value;
+	if (joy_num < 1)
 	{
 		return;
 	}
@@ -2541,7 +2542,6 @@ IN_Controller_Init(qboolean notify_user)
 		}
 	}
 
-	int numjoysticks;
 	const SDL_JoystickID *joysticks = SDL_GetJoysticks(&numjoysticks);
 
 	if (joysticks != NULL)
@@ -2549,7 +2549,6 @@ IN_Controller_Init(qboolean notify_user)
 		Com_Printf ("%i joysticks were found.\n", numjoysticks);
 	}
 
-
 	if (numjoysticks == 0)
 	{
 		joystick_haptic = SDL_OpenHapticFromMouse();
@@ -2581,13 +2580,16 @@ IN_Controller_Init(qboolean notify_user)
 			Com_Printf ("%d mappings loaded from gamecontrollerdb.txt\n", nummappings);
 	}
 
-	for (int i = 0; i < numjoysticks; i++)
+	if (joy_num > numjoysticks) joy_num = 1;
+	joy_num--;
+	i = joy_num;
+	do
 	{
 		joystick = SDL_OpenJoystick(joysticks[i]);
 		if (!joystick)
 		{
 			Com_Printf ("Couldn't open joystick %d: %s.\n", i+1, SDL_GetError());
-			continue;	// try next joystick
+			goto next_joy;	// try next joystick
 		}
 
 		const char* joystick_name = SDL_GetJoystickName(joystick);
@@ -2627,7 +2629,7 @@ IN_Controller_Init(qboolean notify_user)
 				Com_Printf ("skipping.\n");
 			}
 #endif
-			continue;
+			goto next_joy;
 		}
 
 		Com_Printf ("Buttons = %d, Axes = %d, Hats = %d\n", SDL_GetNumJoystickButtons(joystick),
@@ -2656,7 +2658,7 @@ IN_Controller_Init(qboolean notify_user)
 			if (!controller)
 			{
 				Com_Printf("SDL Gamepad error: %s.\n", SDL_GetError());
-				continue;	// try next joystick
+				goto next_joy;	// try next joystick
 			}
 
 			show_gamepad = true;
@@ -2738,7 +2740,12 @@ IN_Controller_Init(qboolean notify_user)
 			break;
 #endif
 		}
+
+next_joy:
+		i++;
+		if (i == numjoysticks) i = 0;
 	}
+	while (i != joy_num);
 
 	SDL_free((void *)joysticks);
 	IN_GamepadLabels_Changed();
