# default audio output
audio.device=alsa_output.platform-hdmi0-sound.stereo-fallback
audio.profile=output:stereo-fallback@alsa_card.platform-hdmi0-sound

# default cpu governor
system.cpu.governor=performance

# Serial port for rtk bluetooth card
system.rtkbt.tty=/dev/ttyS9
