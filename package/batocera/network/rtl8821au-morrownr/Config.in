comment "rtl8821au needs a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL

config BR2_PACKAGE_RTL8821AU_MORROWNR
	bool "rtl8821au-morrownr"
	depends on BR2_LINUX_KERNEL
	help
	  Linux Driver for USB WiFi Adapters that are based on the
	  RTL8811AU and RTL8821AU Chipsets - v5.12.5.2

	  https://github.com/morrownr/8821au-20210708
